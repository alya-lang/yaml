# Types and data structures for yaml
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

struct YamlConfig
    name
    prefix
    count
end

function make_config(name = "World", count = 1)
    return YamlConfig {
        name: name,
        prefix: "Hello",
        count: count
    }
end
