[package]
name = "yaml"
version = "0.1.0"
alya-version = "0.0.19"
entry = "src/lib.alya"
description = "Fast, zero-dependency YAML 1.2 parser and serializer for Alya"
authors = ["Alya Language Contributors <https://github.com/alya-lang>"]
license = "MIT"
homepage = "https://github.com/alya-lang/alya"
repository = "https://github.com/alya-lang/yaml"
keywords = ["alya", "alya-lang", "package", "yaml", "yaml", "parser", "serializer", "config", "data"]

[dependencies]

# [lint]
# disabled_rules = []
# exclude = []
